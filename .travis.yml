language: python
python:
  - 3.3
  - 3.4
  - 3.5
  - 3.6
install:
  - pip install coverage
  - pip install pytest-cov
  - pip install python-coveralls
  - python setup.py install
script:
  - py.test --cov web tests/*
after_success:
  - coveralls
